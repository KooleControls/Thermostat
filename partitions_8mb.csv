# ESP-IDF Partition Table — 8 MB flash (DIYLESS Thermostat 3)
# Two OTA app slots filling the flash. The web UI is embedded in the
# application image (components/web_assets), so there is no www partition:
# firmware and UI are one artifact that updates in one step.
# Name,     Type,   SubType,    Offset,     Size,       Flags
nvs,        data,   nvs,        0x9000,     0x6000,
otadata,    data,   ota,        0xf000,     0x2000,
phy_init,   data,   phy,        0x11000,    0x1000,
ota_0,      app,    ota_0,      0x20000,    0x3F0000,
ota_1,      app,    ota_1,      0x410000,   0x3F0000,
